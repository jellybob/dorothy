<h1><%= @path %></h1>
<ul>
  <% if archives.length > 0 %>
    <% for entry in archives %>
      <li>
        <a href="<%= entry.path %>"><%= entry.title %> (<%= entry.category %>)</a>
      </li>
    <% end %>
  <% end %>
</ul>

